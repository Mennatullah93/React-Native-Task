import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        paddingVertical: 11,
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 17,
        borderRadius: 7,
    },
    text: {
        color: 'white',
        fontWeight: 'bold',
        lineHeight: 24,
    }
});